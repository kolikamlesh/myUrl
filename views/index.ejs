<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Myurl</title>
   
        <style>

            *{
                margin : 0;
                padding : 0;
            }

            body{
                background-color: rgb(102, 7, 218);
                height: 100%;
                width: 100%;
            }

            #panel{
                height : 200px;
                width : 50%;
                text-align: center;
                background-color : green;
                position : absolute;
                top : 100px;
                left : 25%;
                border-radius : 20px;
            }

            #url{
                position : absolute;
                top : 70px;
                height : 40px;
                width : 60%;
                left : 20px;
                text-align : center;
                border-radius : 6px;
            }

            #button{
                height : 40px;
                width : 20%;
                border-radius : 4px;
                font-size : 20px;
                position : absolute;
                top : 70px;
                left : 70%;
            }

            #button:hover{

                background-color: rgb(225, 164, 84);
                font-weight : bold;
                border-radius: 7px;
            }

            #url:hover{
                height : 40px;
                width : 65%;
                font-size: 17px;
            }

            #shorturlbanner{
                height : 40px;
                width : 50%;
                text-align: center;
                background-color : #a1e4fd;
                position : absolute;
                top : 350px;
                left : 25%;
                border-radius : 7px;
            }

            .shortUrl{
                margin-top: 2%;
            }

            #jumbotron{
                padding : 10px;
                border : 2px solid red;
                height : 450px;
                width : 70%;
                margin-left : 15%;
                margin-top : 30px;
                background-color: rgb(234, 154, 191);
            }

            #copy {
                position: absolute;
                top: 70%;
                left: 51%;
                height: 40px;
                width: 80px;
                border-radius: 6px;
                font-size: 18px;
                background-color: #0a0393;
                border: none;
                color: white;
                cursor: pointer;
                transition: background-color 0.3s;
            }

            #redirect{
                position: absolute;
                top: 70%;
                left: 40%;
                height: 40px;
                width: 80px;
                border-radius: 6px;
                font-size: 18px;
                background-color: #0a0393;
                border: none;
                color: white;
                cursor: pointer;
                transition: background-color 0.3s;
            }

            #copy:hover, #redirect:hover {
                background-color: #8507fc;
                color: #0a0393;
                font-weight: bold;
                border-radius: 7px;
            }
  
        </style>
    </head>
    <body>
        
        <div id="jumbotron"> 
            <div id="panel">
                <form action="/createShort" method="post">
                    <input id="url" type="url" name="url" placeholder="https://www.xyz.com/login">
                    <input id='button' type="submit"/>
                </form>
            </div>

            <div class="banner">
                <input type="text" value=<%= responce %> class="shortUrl" id="shorturlbanner">
                <button onclick="copy()" id="copy">copy</button>
                <button onclick="redirect()" id="redirect">redirect</button>
            </div>
        </div>

        <script>

            function copy() {
                // Get the text field
                let copyText = document.getElementById('shorturlbanner')


                // Select the text field
                copyText.select();
                copyText.setSelectionRange(0, 99999); // For mobile devices

                // Copy the text inside the text field
                navigator.clipboard.writeText(copyText.innerHTML);

                // Alert the copied text
                // alert("Copied the text: " + copyText.innerHTML);
            }

            function redirect(){
                window.location.href = document.getElementById('shorturlbanner').value
            }
        </script>
    </body>
</html>